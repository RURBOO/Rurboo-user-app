

class AppRoutes {
  static const String splash = '/';
  static const String home = '/home';
  static const String login = '/login';
  static const String otp = '/otp';
  static const String createProfile = '/create-profile';
  static const String rideSelection = '/ride-selection';
  static const String rideTracking = '/ride-tracking';
}

/// A simple Router class to centralize navigation logic if we move to GoRouter later.
/// For now, it just holds constants to avoid magic strings.
class AppRouter {
  // Future: GoRouter configuration can live here.
}
