rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      // Allow access if the user is signed in
      allow read, write: if request.auth != null;
    }
  }
}
